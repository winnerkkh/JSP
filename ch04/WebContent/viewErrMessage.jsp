<%@ page language="java" contentType="text/html; charset=UTF-8"
	pageEncoding="UTF-8"%>
<%@ page isErrorPage="true"%> <!-- 예외 처리페이지으 지시부에서는 isErrorPage값을 true로함 -->
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>예외발생</title>
</head>
<body>   <!-- 출력할 내용의 크기는 500kb가 넘어야함. -->



	요청처리 과정에서 문제가 발생하였습니다.<br> 
	에러타입: <%=exception.getClass().getName()%><br>
	
	요청처리 과정에서 문제가 발생하였습니다.<br> 
	에러타입: <%=exception.getClass().getName()%><br>
	
	요청처리 과정에서 문제가 발생하였습니다.<br> 
	에러타입: <%=exception.getClass().getName()%><br>
	
	요청처리 과정에서 문제가 발생하였습니다.<br> 
	에러타입: <%=exception.getClass().getName()%><br>
	
	요청처리 과정에서 문제가 발생하였습니다.<br> 
	에러타입: <%=exception.getClass().getName()%><br>
	
	요청처리 과정에서 문제가 발생하였습니다.<br> 
	에러타입: <%=exception.getClass().getName()%><br>
	
	요청처리 과정에서 문제가 발생하였습니다.<br> 
	에러타입: <%=exception.getClass().getName()%><br>
	
	요청처리 과정에서 문제가 발생하였습니다.<br> 
	에러타입: <%=exception.getClass().getName()%><br>
	
	요청처리 과정에서 문제가 발생하였습니다.<br> 
	에러타입: <%=exception.getClass().getName()%><br>
	
	요청처리 과정에서 문제가 발생하였습니다.<br> 
	에러타입: <%=exception.getClass().getName()%><br>
	
	요청처리 과정에서 문제가 발생하였습니다.<br> 
	에러타입: <%=exception.getClass().getName()%><br>
	
	요청처리 과정에서 문제가 발생하였습니다.<br> 
	에러타입: <%=exception.getClass().getName()%><br>
	
	요청처리 과정에서 문제가 발생하였습니다.<br> 
	에러타입: <%=exception.getClass().getName()%><br>
	
	요청처리 과정에서 문제가 발생하였습니다.<br> 
	에러타입: <%=exception.getClass().getName()%><br>	

</body>
</html>